{
  "name": "jenkins-postman",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "generate-server": "java -jar openapi-generator-cli.jar generate -g spring -i openapi.yaml -o api",
    "generate-client": "java -jar openapi-generator-cli.jar generate -g javascript -i openapi.yaml -o client"
  },
  "author": "",
  "license": "ISC"
}
